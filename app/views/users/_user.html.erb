<li>
      <%= link_to user.runame, user %>
        <% if current_user.admin? && !current_user?(user) %>
           |  <%= link_to "Удалить", user, method: :delete,
                                  data: { confirm: "Вы уверены?" } %>
        <% end %>
</li>