<% provide(:title, 'Главная') %>
<div class="row">
  <div class="col-md-6">
    <h5>Информационные сообщения</h5>
    <div class="well">
    </div>
  </div>
  <div class="col-md-6 offset-col-md-6">
    <h5>Общая статистика по объектам</h5>
    <table class="table table-striped table-bordered table-condensed">
      <tr>
        <% if current_user.foreman? %>
        <td class="title_left">Количество УМ</td>
        <% else %>
        <td class="title_left"><%= link_to "Количество УМ", nodes_path %></td>
        <% end %>
        <td><%= Node.count %></td>
      </tr>
      <tr>
        <td class="title_left"><%= link_to "Количество улиц", streets_path %> </td>
        <td><%= Street.count %></td>
      </tr>
      <tr>
        <td class="title_left">Количество домов</td>
        <td><%= Build.count %></td>
      </tr>
      <tr>
        <td class="title_left">Количество подъездов</td>
        <td><%= Porch.count %></td>
      </tr>
      <tr>
        <td class="title_left"><%= link_to "Количество обслуживаемых лифтов", lifts_path %></td>
        <td><%= Lift.count %></td>
      </tr>
    </table>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    <h5>Недавно проведенные ТО:</h5>
    <% if @inspection_last.nil? %>
    <p class="lead">В последнее время ТО не проводилось</p>
    <% else %>
    <div class="well">
      <% @inspection_last.each do |il| %>
      <%= il.inspection_at.strftime("%d.%m.%Y") + ' ' + il.lift.porch.build.street.street_kind.name + ' ' +il.lift.porch.build.street.name + ' - ' + il.lift.porch.build.name + ', ' + il.lift.porch.name %></br>
      <% end %>     
    </div>
    <% end %>
  </div>
  <div class="col-md-6 offset-col-md-6">
    <h5>Ближайшие запланированные ТО:</h5>
    <% if @inspection_next.nil? %>
    <p class="lead">В ближайщее время ТО не запланировано</p>
    <% else %>
    <div class="well">
      <% @inspection_next.each do |il| %>
      <%= il.next_inspection_at.strftime("%d.%m.%Y") + ' ' + il.lift.porch.build.street.street_kind.name + ' ' + il.lift.porch.build.street.name + ' - ' + il.lift.porch.build.name + ', ' + il.lift.porch.name %></br>
      <% end %>
    </div>
    <% end %>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    <h5>Перемещения оборудования:</h5>
    <% if @equipment_movements.nil? %>
      <p class="lead">Перемещений нет</p>
    <% else %>
    <div class="well">
      <%= render @equipment_movements %>
    </div>
    <% end %>
  </div>
  <div class="col-md-6 offset-col-md-6">
    <h5>Изменения в УМ:</h5>
    <% if @nodes_last.nil? %>
    <p class="lead">Перемещений нет</p>
    <% else %>
    <div class="well">
    <% @nodes_last.each do |nl| %> 
     <%= link_to nl.created_at.strftime("%d.%m.%Y") + ' Добавлен УМ' + nl.name, nl %></br>
    <% end %>  
    </div>
    <% end %>
  </div>
</div>