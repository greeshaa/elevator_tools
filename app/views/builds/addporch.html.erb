<% provide(:title, 'Добавление подъезда') %>
<h1>Добавление подъезда</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@porch) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name, "Название подъезда:" %>
      <%= f.text_field :name %>

      <%= f.submit "Добавить", class: "btn btn-mini" %>
    <% end %>
  </div>
</div>