<% provide(:title, @streetkind + ' ' + @street + ', ' + @build.name) %>
<div class="col-md-10 col-md-offset-1">
  <legend><%= @streetkind + ' ' + @street %>, <%= @build.name %></legend>
  <% if @build.id == 1 %>
    <%= render @porches %>
  <% else %>


      <% if @node.nil? %>
        <span class="label label-warning">Дом еще не привязан к какому-либо УМ</span>
      <% elsif @node.porch_id == nil %>
        <% if @node.id == 1 %>
          <span class="label label-warning">Адрес не выводится в ЦД.</span>
        <% else %>
          <p>Дом относится к <%= link_to @node.name, @node %>, который расположен по адресу 
                              <%= @node.street.name %>, <%= @node.build.name %></p>
        <span class="label label-warning">Внимание! Необходимо проверить привязку УМ к подъезду.</span>
        <% end %>
      <% else %>
      <p>Дом относится к <%= link_to @node.name, @node %>, который расположен по адресу <%= @node.street.name %>, 
                        <%= @node.build.name %> в <%= @node.porch.name %> <p>
      <% end %>




      <p>Подъездов в здании: <%= @build.porch_count.to_s %></p>
      <legend>Обслуживаемые лифты</legend>
      <% if @lifts.empty? %>
      <p class="lead">Обслуживаемых лифтов нет.</p>
      <% else %>
        <ul>
          <% @lifts.each do |lift| %>
            <li><%= lift.porch.name + ' - ' %> <%= link_to 'Лифт ' + lift.function + ' - ' + lift.capacity.to_s + ' кг. - '  + lift.stopscount.to_s + ' остановок', lift %>
            </li>
          <% end %>
        </ul>
      <% end %>
  <% end %>
  <div class="btn-group btn-group-sm">
        <%= link_to "Показать все здания улицы", '/streets/' + @streets, class: "btn btn-default" %>
        <%= link_to "Редактировать здание", edit_build_path, class: "btn btn-default" %>
  </div>
</div>